[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "kollabor-plugins"
version = "0.5.10"
description = "Plugin infrastructure for Kollabor"
readme = "README.md"
requires-python = ">=3.12"
license = {text = "MIT"}
dependencies = [
    "kollabor-events>=0.5.10",
]

[tool.hatch.build.targets.wheel]
packages = ["src/kollabor_plugins"]
