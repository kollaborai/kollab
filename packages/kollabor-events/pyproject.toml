[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "kollabor-events"
version = "0.5.15"
description = "Event bus and hook system for Kollabor"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/kollabor_events"]
