name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  tests:
    name: ci/tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0

      - name: Install workspace
        run: uv sync --all-packages --extra dev

      # Run the whole unit suite, not a hand-listed subset. The old list named
      # tests/unit/test_turn_runner_tool_loop.py, which was deleted with the
      # turn_runner module in dca5e73 -- pytest then exited 4 ("collected 0
      # items") and every other file in the list was skipped too, so CI
      # reported failure without ever running a test. A directory can't rot
      # that way, and it covers ~2700 tests instead of 8.
      - name: Run unit tests
        run: uv run python -m pytest tests/unit/ -q

      - name: Validate bundled Agent Skills layout
        run: uv run python scripts/validate_bundled_agent_skills.py

  standards-check:
    name: ci/standards-check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0

      - name: Install workspace
        run: uv sync --all-packages --extra dev

      - name: Check syntax-critical lint rules
        run: uv run python -m ruff check --select E9,F63,F7,F82 kollabor packages plugins tests

      - name: Compile public entry points
        run: uv run python -m py_compile kollabor/cli.py kollabor_cli_main.py plugins/hub/plugin.py

  security-scan:
    name: ci/security-scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install Gitleaks
        env:
          GITLEAKS_VERSION: "8.30.1"
        run: |
          cd "$RUNNER_TEMP"
          curl -fsSLO "https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}/gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz"
          curl -fsSLO "https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}/gitleaks_${GITLEAKS_VERSION}_checksums.txt"
          grep "gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz" "gitleaks_${GITLEAKS_VERSION}_checksums.txt" | sha256sum -c -
          mkdir -p "$RUNNER_TEMP/gitleaks-bin"
          tar -xzf "gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz" -C "$RUNNER_TEMP/gitleaks-bin" gitleaks
          echo "$RUNNER_TEMP/gitleaks-bin" >> "$GITHUB_PATH"

      - name: Run Gitleaks
        run: gitleaks detect --source . --redact --verbose
