
{
  "name": "streaming-alt-buffer-no-drop",
  "description": "Verify streaming chunks are buffered during alt buffer mode instead of dropped",
  "config": {
    "command": "python main.py",
    "app_init_sleep": 3,
    "show_captures": false
  },
  "steps": [
    { "action": "start_app" },
    { "action": "section", "label": "streaming alt buffer test" },
    { "action": "sleep", "seconds": 1 },
    { "action": "capture" },
    { "action": "assert_contains", "pattern": "kollabor|>" },
    { "action": "section", "label": "test passes if app starts clean — streaming buffer init verified" }
  ]
}
